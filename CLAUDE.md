@AGENTS.md
@HANDOFF.md

# rakuten-room — Claude Code向けの入口（参照専用）

**このプロジェクトの通常運用はCodexが担当**しており、ルール・運用の**正本は `AGENTS.md`**、現在の状態は `HANDOFF.md`（冒頭で自動読込済み）。このファイルは、Claude Codeで開いた場合でも同じ正本に確実に到達させるための薄い入口であり、ここに固有ルールは書かない（二重管理しない）。

- 本部ルール（`C:\Users\daiku\ai-company\CLAUDE.md`）を継承する。特に7項目承認ルール（削除・commit/push・デプロイ・DB変更・認証/課金まわり・秘密情報・大量置換＝必ず事前に社長承認）は不変
- Codexの作業と衝突させないこと：ローカルワーカー・Windowsタスク・`.local/` 配下・認証プロファイルには触れない。修正が必要と判断したら、変更せず社長へ報告する
- このリポジトリは**公開リポジトリ**（GitHub Pages配信のため・意図的）。秘密情報・個人情報は絶対に置かない
